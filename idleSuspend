#!/bin/bash

#timeout=$(xset q | grep timeout | cut -d ' ' -f 5) * 1000
timeout=30000

while true
do
    time=$(xprintidle)
    echo $time
    if [ $time -gt $timeout ];
    then
        systemctl suspend
    fi
    sleep 60
done
